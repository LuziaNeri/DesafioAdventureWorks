version: 2

models:
  - name: dim_locations
    columns:
      - name: sk_locations
        description: A chave substituta da tabela.
        tests:
          - not_null
          - unique

      - name: id_estado
        description: Id do endereço do estado.
        tests:
          - not_null          

      - name: id_territorio
        description: Id do endereço do território.
        tests:
          - not_null
          

      - name: nome_cidade
        description: O nome da cidade.
        tests:
          - not_null
      
      - name: nome_estado
        description: O nome do estado.
        tests:
          - not_null

      - name: codigo_pais
        description: O código do país.
        tests:
          - not_null
      
      - name: nome_pais
        description: O código do país.
        tests:
          - not_null